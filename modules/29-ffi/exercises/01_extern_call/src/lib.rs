//! 01 (0x) - вызов функции C из Rust через `extern "C"` (раздел 2 README).
//!
//! Функции стандартной библиотеки C доступны без настройки линковки: libc уже
//! слинкована. Нужно лишь объявить сигнатуру во внешнем блоке и вызвать из `unsafe`.
//!
//! Сделай:
//! - объяви блок `extern "C" { ... }` с функцией C `int abs(int)`. Используй тип
//!   `c_int` из `std::os::raw` для аргумента и результата.
//! - `libc_abs(x)` - вызвать объявленную `abs` (вызов внешней функции всегда `unsafe`).
//! - `abs_diff(a, b)` - модуль разности `a` и `b`; выразить через `libc_abs`, разность
//!   считать через `wrapping_sub`, чтобы не переполниться.
//!
//! Почему вызов `unsafe`: компилятор не видит тело `abs` и верит объявленной сигнатуре
//! на слово - за её точность отвечает программист.
//!
//! Справка по внешним блокам:
//! <https://doc.rust-lang.org/reference/items/external-blocks.html>. C-типы:
//! <https://doc.rust-lang.org/std/os/raw/index.html>.

use std::os::raw::c_int;

// TODO: объяви здесь extern "C" блок с функцией abs.

/// Модуль числа через libc `abs`.
pub fn libc_abs(x: c_int) -> c_int {
    let _ = x;
    todo!()
}

/// Модуль разности `a - b`.
pub fn abs_diff(a: c_int, b: c_int) -> c_int {
    let _ = (a, b);
    todo!()
}
