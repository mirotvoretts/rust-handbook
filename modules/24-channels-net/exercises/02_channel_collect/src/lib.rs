//! 02 (0x) - итерация Receiver, сбор с сохранением порядка.
//!
//! Производитель в отдельном потоке отправляет квадраты элементов `input` в том же
//! порядке, в каком они идут во входе. Потребитель собирает всё пришедшее в Vec.
//! У одного производителя порядок сообщений сохраняется - результат должен идти в
//! исходном порядке (сортировать не нужно).

use std::sync::mpsc;
use std::thread;

/// Отправь квадраты элементов `input` через канал и собери их в вектор в исходном
/// порядке.
pub fn collect_squares(input: Vec<i64>) -> Vec<i64> {
    todo!()
}
