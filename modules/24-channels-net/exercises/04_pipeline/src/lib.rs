//! 04 (1x) - двухстадийный конвейер (pipeline) на двух каналах.
//!
//! Собери конвейер из трёх узлов, соединённых двумя каналами:
//!   источник --канал1--> воркер --канал2--> сток
//! Источник (поток) шлёт элементы `input` в канал1. Воркер (поток) читает из канала1,
//! применяет к каждому элементу `f` и шлёт результат в канал2. Сток (текущий поток)
//! собирает из канала2 итоговый вектор в исходном порядке.
//!
//! Все три узла работают одновременно, данные текут по конвейеру.

use std::sync::mpsc;
use std::thread;

/// Пропусти каждый элемент `input` через воркер, применяющий `f`, по конвейеру из
/// двух каналов и верни результаты в исходном порядке.
pub fn pipeline<F>(input: Vec<i64>, f: F) -> Vec<i64>
where
    F: Fn(i64) -> i64 + Send + 'static,
{
    todo!()
}
