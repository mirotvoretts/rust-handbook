//! 03 (1x) - клонирование Sender: несколько производителей, один потребитель.
//!
//! На каждый под-вектор из `chunks` запусти отдельный поток; поток считает сумму
//! своей части и отправляет её в общий канал. Главный поток принимает все частичные
//! суммы и складывает в итог. Клонируй Sender по разу на поток. Не забудь про
//! исходный Sender: пока он жив, канал не закроется и приём не завершится.

use std::sync::mpsc;
use std::thread;

/// Просуммируй все элементы всех под-векторов, посчитав частичные суммы в отдельных
/// потоках и собрав их через канал.
pub fn parallel_sum(chunks: Vec<Vec<i64>>) -> i64 {
    todo!()
}
