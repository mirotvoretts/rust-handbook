//! 01 (0x) - mpsc::channel: производитель шлёт числа, потребитель суммирует.
//!
//! Создай канал, в отдельном потоке отправь по нему все значения `values`, а в
//! текущем потоке принимай их и суммируй. Итерация по Receiver заканчивается сама,
//! когда отправитель (Sender) уничтожен, - воспользуйся этим вместо ручного счёта.

use std::sync::mpsc;
use std::thread;

/// Отправь каждое значение `values` через канал из потока-производителя и верни
/// сумму всех принятых значений.
pub fn sum_over_channel(values: Vec<i64>) -> i64 {
    todo!()
}
