//! 04 (1x) - много потоков: сбор хендлов и результатов по порядку.
//!
//! На каждый элемент входа запустите свой поток. Хендлы сложите в коллекцию, а затем,
//! пройдя по ней, соедините каждый и соберите результаты. Порядок результата должен
//! совпадать с порядком входа - подумайте, за счёт чего это гарантируется (spawn всех
//! ДО того, как join первого, и сохранение порядка хендлов).

use std::thread;

/// Для каждого x из xs посчитай x * x в ОТДЕЛЬНОМ потоке и верни вектор квадратов
/// в исходном порядке.
pub fn squares(xs: Vec<i64>) -> Vec<i64> {
    todo!()
}
