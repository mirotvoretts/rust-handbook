//! 08 (3x) - непересекающиеся &mut через scope: изменение среза на месте.
//!
//! Общий &mut в два потока запрещён - но если поделить срез на НЕПЕРЕСЕКАЮЩИЕСЯ
//! изменяемые куски, каждый поток может писать в свой кусок безопасно. Нарежьте срез на
//! parts изменяемых кусков (существует адаптер, дающий именно непересекающиеся &mut
//! подсрезы), в thread::scope запустите поток на каждый и удвойте его элементы на месте.
//! Считайте parts >= 1; пустой срез - пустая работа.

use std::thread;

/// Удвой каждый элемент среза на месте, распределив работу по не более чем parts потокам.
pub fn parallel_double(xs: &mut [i32], parts: usize) {
    todo!()
}
