//! 05 (2x) - thread::scope: параллельная сумма двух половин без передачи владения.
//!
//! Здесь данные приходят по ссылке (&[i64]) - забрать их во владение нельзя, а обычный
//! spawn заимствовать их не даст (нужен 'static). thread::scope создаёт область, из
//! которой все порождённые потоки гарантированно завершатся, поэтому им РАЗРЕШЕНО
//! заимствовать xs. Разбейте срез на две части, посчитайте каждую в своём потоке области
//! и сложите результаты.

use std::thread;

/// Сумма среза, посчитанная в двух потоках: одна половина в одном, вторая в другом.
pub fn parallel_sum(xs: &[i64]) -> i64 {
    todo!()
}
