//! 07 (3x) - изоляция паники: успешные и упавшие задачи.
//!
//! Паника раскручивает стек только своего потока и не роняет остальные. Запустите поток
//! на каждую задачу; поток ДОЛЖЕН запаниковать, если значение задачи отрицательное, и
//! завершиться нормально иначе. Соединяя хендлы, отличите нормальное завершение от паники
//! (join сообщает о панике через свой результат) и посчитайте тех и других.

use std::thread;

/// Запусти по потоку на каждый элемент jobs. Поток паникует, если его число < 0, иначе
/// завершается нормально. Верни (число успешных, число упавших).
pub fn run_jobs(jobs: Vec<i32>) -> (usize, usize) {
    todo!()
}
