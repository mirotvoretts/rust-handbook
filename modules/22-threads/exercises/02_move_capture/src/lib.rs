//! 02 (0x) - move-замыкание: передача владения данными в поток.
//!
//! Замыкание потока должно владеть тем, что использует: дочерний поток может пережить
//! текущую область, поэтому одалживать ссылку нельзя. Подумайте, какое ключевое слово
//! перед замыканием заставляет его забрать захваченные значения во владение.

use std::thread;

/// Передай msg во владение отдельному потоку, там переведи строку в верхний регистр
/// (to_uppercase) и верни результат обратно через join.
pub fn shout_in_thread(msg: String) -> String {
    todo!()
}
