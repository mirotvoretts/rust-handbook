//! 03 (1x) - переместить владеющие данные в поток и вернуть результат вычисления.
//!
//! Вектор нужно отдать потоку во владение (после запуска он больше не доступен снаружи).
//! Поток считает сумму и возвращает её; заберите значение через join.

use std::thread;

/// Перемести xs в отдельный поток, посчитай там сумму элементов (как i64) и верни её.
pub fn spawn_sum(xs: Vec<i32>) -> i64 {
    todo!()
}
