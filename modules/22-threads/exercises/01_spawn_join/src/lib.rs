//! 01 (0x) - spawn + join: запуск потока и получение его результата.
//!
//! thread::spawn запускает замыкание в отдельном потоке и возвращает хендл. join на
//! хендле блокирует текущий поток до завершения запущенного и отдаёт то, что вернуло
//! замыкание (обёрнутое в Result - обработайте его так, чтобы вернуть само число).

use std::thread;

/// Запусти отдельный поток, который вычисляет a + b, дождись его и верни сумму.
pub fn spawn_add(a: i32, b: i32) -> i32 {
    todo!()
}
