//! 08 (2x) - Срезы по границам символов.
//!
//! &s[..n] паникует, если n попал в середину UTF-8 символа. Обрезайте безопасно:
//! char_indices даёт байтовые смещения НАЧАЛ символов - по ним резать можно всегда.

/// Обрезает строку до max_bytes БАЙТ, не разрывая символы; добавляет "…" если резали.
/// Гарантия: результат (без "…") - валидный префикс строки <= max_bytes байт.
pub fn truncate_bytes(s: &str, max_bytes: usize) -> String {
    todo!("s.char_indices() - найдите последнюю границу i, где i + len_utf8 <= max_bytes; или is_char_boundary")
}

/// Байтовое смещение начала n-го символа (None, если символов меньше).
pub fn char_offset(s: &str, n: usize) -> Option<usize> {
    todo!("char_indices().nth(n)")
}
