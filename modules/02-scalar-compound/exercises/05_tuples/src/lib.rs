//! 05 (1x) — Кортежи: возврат нескольких значений и деструктуризация.
//!
//! Кортеж `(A, B)` — анонимное произведение. Идиоматичный способ вернуть несколько значений.
//! Разбирайте вход через `let (a, b) = ...`.

/// Частное и остаток от деления: (a / b, a % b).
pub fn divmod(a: i32, b: i32) -> (i32, i32) {
    todo!("(a / b, a % b)")
}

/// Минимум и максимум трёх чисел в виде (min, max).
pub fn min_max(a: i32, b: i32, c: i32) -> (i32, i32) {
    todo!()
}

/// Меняет местами элементы пары: (x, y) -> (y, x). Разберите вход деструктуризацией.
pub fn swap_pair(p: (i32, i32)) -> (i32, i32) {
    todo!("let (x, y) = p; (y, x)")
}
