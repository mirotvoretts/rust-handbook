//! 03 (0x) — Числа с плавающей точкой `f64`.
//!
//! `f64` нельзя сравнивать на точное равенство (ошибки округления). Сравнивайте через ε.

/// Среднее двух чисел.
pub fn average(a: f64, b: f64) -> f64 {
    todo!("(a + b) / 2.0")
}

/// Близки ли числа: истина, если |a - b| <= eps.
pub fn is_close(a: f64, b: f64, eps: f64) -> bool {
    todo!("(a - b).abs() <= eps")
}

/// Длина гипотенузы по катетам: sqrt(a² + b²). Подсказка: у `f64` есть метод `.sqrt()`.
pub fn hypotenuse(a: f64, b: f64) -> f64 {
    todo!()
}
