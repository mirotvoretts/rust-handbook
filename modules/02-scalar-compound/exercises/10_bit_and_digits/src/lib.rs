//! 10 (2x) — Биты и цифры.
//!
//! Работа с целым как с набором битов и как с десятичной записью.

/// Число единичных битов в двоичной записи (популяция). count_set_bits(7) == 3.
/// Можно вручную (сдвиги и `& 1`) или методом `n.count_ones()`.
pub fn count_set_bits(n: u32) -> u32 {
    todo!()
}

/// Сумма десятичных цифр: 123 -> 6, 9999 -> 36, 0 -> 0.
pub fn digit_sum(n: u32) -> u32 {
    todo!()
}

/// Является ли число степенью двойки (1, 2, 4, 8, ...). Ноль — нет.
/// Классический трюк: `n != 0 && n & (n - 1) == 0`.
pub fn is_power_of_two(n: u32) -> bool {
    todo!()
}
