//! 11 (2x) — Классификация символов и шифр Цезаря.
//!
//! У `char` есть методы `is_alphabetic()`, `is_numeric()`, `is_ascii_lowercase()` и др.
//! Для арифметики над буквами удобно переходить через `u8` (ASCII-код) и обратно через `as`.

/// Считает (число буквенных символов, число числовых символов) в строке.
/// Внимание: буквы — не только ASCII (кириллица тоже `is_alphabetic`).
pub fn count_alpha_digit(s: &str) -> (usize, usize) {
    todo!("пройдитесь по s.chars(), считая is_alphabetic и is_numeric")
}

/// Шифр Цезаря: сдвигает ASCII-буквы на `k` позиций внутри своего регистра (с обёрткой по
/// алфавиту), остальные символы оставляет как есть.
/// 'a'+1 -> 'b', 'z'+1 -> 'a', 'A'+2 -> 'C', '!'+5 -> '!'.
/// Подсказка: приведите k к диапазону 0..26 заранее, чтобы избежать переполнения u8.
pub fn caesar_shift(c: char, k: u8) -> char {
    todo!()
}
