//! 04 (0x) - `char` и `bool`.
//!
//! `char` - Unicode-скаляр (4 байта), у него много методов-классификаторов. `bool` - только
//! `true`/`false`, без неявного приведения из чисел.

/// Является ли символ строчной латинской гласной (a, e, i, o, u).
pub fn is_vowel(c: char) -> bool {
    todo!("сопоставьте c с гласными")
}

/// Числовое значение цифры в десятичной системе: '7' -> Some(7), 'x' -> None.
/// Подсказка: у `char` есть метод `to_digit(10)`.
pub fn digit_value(c: char) -> Option<u32> {
    todo!()
}

/// Исключающее ИЛИ двух логических значений.
pub fn xor(a: bool, b: bool) -> bool {
    todo!("a ^ b")
}
