//! 02 (0x) — Явные операции при переполнении.
//!
//! По умолчанию переполнение паникует (debug) или оборачивается (release). Вместо того чтобы
//! полагаться на сборку, выбирайте явную операцию: `wrapping_*`, `checked_*`, `saturating_*`.

/// Сложение по модулю 256 (обёртка): 250 + 10 -> 4. Подсказка: `a.wrapping_add(b)`.
pub fn wrapping_add_u8(a: u8, b: u8) -> u8 {
    todo!()
}

/// Сложение с проверкой: `Some(sum)` либо `None` при переполнении. Подсказка: `a.checked_add(b)`.
pub fn checked_add_u8(a: u8, b: u8) -> Option<u8> {
    todo!()
}

/// Вычитание с «прилипанием» к границе: 3 - 10 -> 0 (не паника, не обёртка).
pub fn saturating_sub_u8(a: u8, b: u8) -> u8 {
    todo!()
}
