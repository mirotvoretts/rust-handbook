//! 01 (0x) — Целые типы: границы и приведение `as`.
//!
//! `as` приводит числовые типы БЕЗ проверок: сужение берёт младшие биты, расширение
//! безопасно. Границы типа доступны как `T::MAX` / `T::MIN`.

/// Максимальное значение `i8` (подсказка: `i8::MAX`).
pub fn i8_max() -> i8 {
    todo!()
}

/// Приводит `i32` к `u8`, оставляя младшие 8 бит (например, 300 -> 44, -1 -> 255).
pub fn truncate_to_u8(n: i32) -> u8 {
    todo!("n as u8")
}

/// Расширяет `u8` до `u64` без потери значения.
pub fn widen(n: u8) -> u64 {
    todo!()
}
