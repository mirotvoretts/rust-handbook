//! 07 (1x) - Приватные поля + публичные методы (инкапсуляция).
//!
//! Поле `balance` приватно, поэтому снаружи модуля его нельзя ни прочитать, ни изменить
//! напрямую - только через публичные методы. Это защищает инвариант (баланс меняется лишь
//! контролируемо). Реализуйте методы.

pub mod bank {
    pub struct Account {
        balance: u64, // приватное поле
    }

    impl Account {
        /// Создаёт счёт с начальным балансом.
        pub fn new(initial: u64) -> Account {
            todo!()
        }

        /// Текущий баланс.
        pub fn balance(&self) -> u64 {
            todo!()
        }

        /// Пополняет счёт на `amount`.
        pub fn deposit(&mut self, amount: u64) {
            todo!()
        }
    }
}
