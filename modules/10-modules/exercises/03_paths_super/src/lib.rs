//! 03 (0x) — Обращение к родительскому модулю через `super::`.
//!
//! Из вложенного модуля к элементам родителя обращаются через `super::` (аналог `../`).
//! Реализуйте `child::plus_base`, вызвав `super::base_value()`.

/// Базовое значение, объявленное в корне крейта.
pub fn base_value() -> i32 {
    10
}

pub mod child {
    /// Возвращает n + базовое значение из родительского модуля.
    pub fn plus_base(n: i32) -> i32 {
        todo!("n + super::base_value()")
    }
}
