//! 01 (0x) — Объявление модуля с публичными функциями.
//!
//! Модуль `arithmetic` уже объявлен. Реализуйте его публичные функции. Тесты обращаются к ним
//! по пути `arithmetic::add` и `arithmetic::mul`.

pub mod arithmetic {
    /// Сумма двух чисел.
    pub fn add(a: i32, b: i32) -> i32 {
        todo!()
    }

    /// Произведение двух чисел.
    pub fn mul(a: i32, b: i32) -> i32 {
        todo!()
    }
}
