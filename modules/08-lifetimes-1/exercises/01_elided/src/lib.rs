//! 01 (0x) - Элизия: аннотации не нужны.
//!
//! У этих функций один входной параметр-ссылка, поэтому по правилу элизии №2 время жизни
//! результата совпадает со входом автоматически - писать `'a` не требуется.

/// Первый символ строки как срез `&str` (для пустой строки - пустой срез).
/// Возвращается СРЕЗ входа, без копирования.
pub fn head(s: &str) -> &str {
    todo!("возьмите длину первого символа c.len_utf8() и верните s[..len]")
}

/// Всё, кроме первого символа (для пустой строки - пустой срез).
pub fn without_head(s: &str) -> &str {
    todo!()
}
