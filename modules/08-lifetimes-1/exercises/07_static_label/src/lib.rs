//! 07 (1x) — Возврат `&'static str`.
//!
//! Строковые литералы вшиты в бинарник и живут всю программу — их тип `&'static str`.
//! Такую ссылку можно вернуть без привязки ко входу.

/// Категория размера по числу: 0..=9 -> "small", 10..=99 -> "medium", иначе -> "large".
pub fn size_name(n: u32) -> &'static str {
    todo!("match по диапазонам, возвращая строковые литералы")
}
