//! 04 (0x) - Выбор одной из двух ссылок по флагу.
//!
//! Оба входа и результат разделяют время жизни `'a`: результат - это одна из входных ссылок,
//! поэтому нельзя использовать его дольше, чем живёт наиболее короткоживущий вход.

pub fn pick<'a>(first: &'a str, second: &'a str, use_first: bool) -> &'a str {
    todo!("верните first или second в зависимости от use_first")
}
