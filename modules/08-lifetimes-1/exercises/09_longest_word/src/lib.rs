//! 09 (2x) - Самое длинное слово из среза строк.
//!
//! Вход - срез строковых срезов `&[&'a str]`. Результат - ссылка на одно из этих слов, поэтому
//! его время жизни `'a` привязано к самим словам (аннотация задана явно: элизия тут не помогает).

/// Самое длинное слово (по числу байт). При равенстве - первое из максимальных.
/// Для пустого среза - `None`.
pub fn longest_word<'a>(words: &[&'a str]) -> Option<&'a str> {
    todo!("пройдитесь по words, запоминая самое длинное")
}
