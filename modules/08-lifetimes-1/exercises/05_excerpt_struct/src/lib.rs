//! 05 (1x) — Структура с полем-ссылкой (`<'a>` уже задан).
//!
//! `Excerpt` не владеет строкой, а лишь ссылается на её кусок. Параметр времени жизни `'a`
//! уже объявлен — реализуйте методы. Экземпляр `Excerpt` не может пережить строку, на которую
//! смотрит `part`.

pub struct Excerpt<'a> {
    pub part: &'a str,
}

impl<'a> Excerpt<'a> {
    /// Создаёт выдержку из среза строки.
    pub fn new(part: &'a str) -> Excerpt<'a> {
        todo!()
    }

    /// Возвращает хранимый срез.
    pub fn part(&self) -> &str {
        todo!()
    }

    /// Длина выдержки в байтах.
    pub fn len(&self) -> usize {
        todo!()
    }
}
