//! 11 (2x) - Метод, возвращающий более длинное из двух полей-ссылок.
//!
//! `Pair` хранит две ссылки с общим временем жизни `'a`. Метод `longer` возвращает ту из них,
//! что длиннее - её тип `&'a str` (а не время жизни `&self`), потому что данные живут `'a`.

pub struct Pair<'a> {
    pub a: &'a str,
    pub b: &'a str,
}

impl<'a> Pair<'a> {
    pub fn new(a: &'a str, b: &'a str) -> Pair<'a> {
        Pair { a, b }
    }

    /// Более длинное из двух полей (при равенстве - `a`).
    pub fn longer(&self) -> &'a str {
        todo!()
    }
}
