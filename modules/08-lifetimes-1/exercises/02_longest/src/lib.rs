//! 02 (0x) - ДОБАВЬТЕ `'a` САМИ.
//!
//! Этот крейт НЕ КОМПИЛИРУЕТСЯ: `error[E0106]: missing lifetime specifier`. У функции два
//! входных параметра-ссылки, и компилятор не знает, с каким из них связать результат.
//! Добавьте параметр времени жизни `<'a>` и пометьте им оба входа и выход (`&'a str`), затем
//! реализуйте тело: вернуть более длинную строку (по числу байт; при равенстве - `x`).

pub fn longest(x: &str, y: &str) -> &str {
    todo!("верните x при x.len() >= y.len(), иначе y")
}
