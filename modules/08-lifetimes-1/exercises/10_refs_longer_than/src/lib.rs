//! 10 (2x) — Собрать ссылки на слова длиннее порога.
//!
//! Результат — вектор ССЫЛОК на слова из входа, поэтому его элементы разделяют время жизни `'a`
//! с входными словами. Сам `Vec` владеет ссылками, но не строками.

/// Возвращает слова строго длиннее `min_len` байт, в исходном порядке.
pub fn refs_longer_than<'a>(words: &[&'a str], min_len: usize) -> Vec<&'a str> {
    todo!("отфильтруйте words по длине и соберите в Vec")
}
