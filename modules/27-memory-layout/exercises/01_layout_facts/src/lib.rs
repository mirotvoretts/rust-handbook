//! 01 (0x) - размер, выравнивание и padding (раздел 1 README).
//!
//! Реализуй функции, возвращающие layout нескольких типов. Числа бери не константами
//! "с потолка", а через `std::mem::size_of` и `std::mem::align_of` для нужного типа -
//! так ответ считает компилятор, а не ты.
//!
//! - `u64_layout` -> `(размер, выравнивание)` типа `u64`;
//! - `pair_layout` -> `(размер, выравнивание)` кортежа `(u8, u32)`;
//! - `pair_padding` -> сколько байт в `(u8, u32)` занял padding (это размер кортежа
//!   минус сумма размеров его полей `u8` и `u32`);
//! - `array_layout` -> `(размер, выравнивание)` массива `[u16; 5]`.

/// Размер и выравнивание `u64`.
pub fn u64_layout() -> (usize, usize) {
    todo!()
}

/// Размер и выравнивание кортежа `(u8, u32)`.
pub fn pair_layout() -> (usize, usize) {
    todo!()
}

/// Сколько байт padding в кортеже `(u8, u32)`.
pub fn pair_padding() -> usize {
    todo!()
}

/// Размер и выравнивание массива `[u16; 5]`.
pub fn array_layout() -> (usize, usize) {
    todo!()
}
