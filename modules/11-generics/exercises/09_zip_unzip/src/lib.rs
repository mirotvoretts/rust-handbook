//! 09 (2x) — Владение и два параметра типа.
//!
//! `zip_pairs` склеивает два вектора в вектор пар (длина — минимум из двух), `unzip_pairs`
//! разбирает обратно. Обе функции ЗАБИРАЮТ владение аргументами и отдают его в результате —
//! ни одного clone.

/// [a1,a2,...] + [b1,b2,...] -> [(a1,b1), (a2,b2), ...]; длина = min(len_a, len_b).
pub fn zip_pairs<A, B>(xs: Vec<A>, ys: Vec<B>) -> Vec<(A, B)> {
    todo!("into_iter().zip(...) — или цикл с двумя итераторами")
}

/// [(a1,b1), ...] -> ([a1,...], [b1,...]).
pub fn unzip_pairs<A, B>(pairs: Vec<(A, B)>) -> (Vec<A>, Vec<B>) {
    todo!("for (a, b) in pairs — паттерн в for тоже работает")
}
