//! 03 (0x) — Turbofish и вывод типа результата.
//!
//! `str::parse` и `Iterator::collect` обобщены по ВОЗВРАЩАЕМОМУ типу: без подсказки
//! компилятор не знает, что вы хотите получить. Подсказать можно turbofish'ем
//! (`parse::<i32>()`) или аннотацией let-привязки. Попробуйте оба способа.

/// Парсит каждую строку как i32; непарсящиеся строки пропускает.
pub fn parse_all(strs: &[&str]) -> Vec<i32> {
    todo!("s.parse::<i32>() возвращает Result — фильтруйте через if let Ok(...)")
}

/// Числа от 1 до n включительно, собранные в вектор.
pub fn one_to_n(n: u32) -> Vec<u32> {
    todo!("(1..=n).collect::<Vec<u32>>() — или аннотация на let")
}

/// Сумма всех корректных i32 в строках (пустой ввод -> 0).
pub fn sum_parsed(strs: &[&str]) -> i64 {
    todo!("переиспользуйте parse_all, суммируйте в i64 (as i64)")
}
