//! 01 (0x) — Первые обобщённые функции.
//!
//! Никаких ограничений (bounds) здесь не нужно: все три функции работают с «голым» `T` —
//! только перемещение и займы. Если ловите себя на мысли «мне нужен Clone» — вы решаете
//! не тем способом.

/// Возвращает аргумент как есть.
pub fn identity<T>(x: T) -> T {
    todo!("самая короткая функция курса")
}

/// Ссылка на первый элемент среза (или None для пустого).
pub fn first<T>(xs: &[T]) -> Option<&T> {
    todo!("сопоставьте с пустотой: xs.is_empty() или метод .first()")
}

/// Ссылка на последний элемент среза (или None для пустого).
pub fn last<T>(xs: &[T]) -> Option<&T> {
    todo!("индекс len()-1 — аккуратно с пустым срезом")
}
