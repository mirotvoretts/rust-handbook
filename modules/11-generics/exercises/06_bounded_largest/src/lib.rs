//! 06 (1x) — Минимальные ограничения.
//!
//! Сравнение `>` требует способности `PartialOrd` — запросите её в сигнатуре. Возвращаем
//! ССЫЛКИ (&T), а не T: так не нужны ни Copy, ни Clone. Правило зрелого кода: просите у T
//! ровно столько, сколько нужно телу функции, и ни ограничением больше.

/// Ссылка на наибольший элемент (None для пустого среза).
/// При нескольких равных максимумах — первый из них.
pub fn largest<T: PartialOrd>(xs: &[T]) -> Option<&T> {
    todo!("пройдитесь по срезу, храните ссылку на лучшего кандидата")
}

/// Ссылка на наименьший элемент (None для пустого среза).
pub fn smallest<T: PartialOrd>(xs: &[T]) -> Option<&T> {
    todo!()
}

/// Отсортирован ли срез по неубыванию? (пустой и одноэлементный — да)
pub fn is_sorted<T: PartialOrd>(xs: &[T]) -> bool {
    todo!("сравнивайте соседей xs[i-1] <= xs[i]")
}
