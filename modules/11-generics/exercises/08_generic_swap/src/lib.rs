//! 08 (2x) - Мутация без лишних ограничений.
//!
//! Все три функции переставляют или заменяют значения, НЕ создавая новых, - поэтому им не
//! нужны ни Clone, ни Copy, ни Default. Инструменты: `std::mem::swap`, `std::mem::replace`
//! (повторите §8 модуля M06).

/// Меняет местами первый и последний элементы. Для len < 2 - ничего не делает.
pub fn swap_ends<T>(xs: &mut [T]) {
    todo!("indices 0 и len-1; slice имеет метод .swap(i, j) - или mem::swap")
}

/// Кладёт new в слот, возвращает старое значение.
pub fn exchange<T>(slot: &mut T, new: T) -> T {
    todo!("std::mem::replace")
}

/// Разворачивает срез на месте (первый <-> последний, второй <-> предпоследний, ...).
pub fn reverse_in_place<T>(xs: &mut [T]) {
    todo!("два индекса с концов навстречу; xs.swap(i, j)")
}
