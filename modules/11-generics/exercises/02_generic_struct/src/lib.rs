//! 02 (0x) — Обобщённая структура.
//!
//! Структура `Pair<T>` уже объявлена: два публичных поля одного типа `T`. Реализуйте две
//! свободные функции. Обратите внимание: `Pair<i32>` и `Pair<&str>` — разные типы, но код
//! вы пишете один раз.

/// Пара значений одного типа.
pub struct Pair<T> {
    pub left: T,
    pub right: T,
}

/// Собирает пару из двух значений.
pub fn make_pair<T>(left: T, right: T) -> Pair<T> {
    todo!("литерал структуры")
}

/// Разбирает пару обратно в кортеж.
pub fn into_tuple<T>(p: Pair<T>) -> (T, T) {
    todo!("доступ к полям перемещает значения из p")
}
