//! 11 (3x) — anyhow: ошибки приложения.
//!
//! В приложении точный тип ошибки не важен — важен понятный отчёт. anyhow::Result
//! принимает ЛЮБУЮ Error-ошибку через ?, а .context()/.with_context() наслаивает
//! объяснения. Тесты проверяют, что контекст попал в цепочку (format!("{:#}", err)
//! печатает всю цепочку через ": ").

use anyhow::{Context, Result};

/// Разбирает строку "число число число ..." и возвращает среднее.
/// Ошибки: пустой ввод (anyhow::bail! или .context) и непарсящийся токен —
/// с контекстом, содержащим сам токен.
pub fn mean_of_line(line: &str) -> Result<f64> {
    todo!("parse::<f64>().with_context(|| ...) для каждого токена")
}

/// Среднее по нескольким строкам, с контекстом «строка N» при ошибке.
pub fn mean_per_line(lines: &[&str]) -> Result<Vec<f64>> {
    todo!("mean_of_line + with_context, добавляющий номер строки")
}
