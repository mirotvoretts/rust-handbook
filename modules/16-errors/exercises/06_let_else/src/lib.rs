//! 06 (1x) - let else.
//!
//! "Распакуй или выйди": паттерн слева, обязательная расходящаяся else-ветка.
//! Перепишите обе функции с let else (тесты поведение зафиксировали; решение
//! обязано использовать let else - на слово, компилятор это не проверит).

/// Первое число из строки вида "count: 42; ...". Нет числа -> "n/a".
pub fn extract_count(line: &str) -> String {
    todo!("let Some(rest) = line.strip_prefix(...) else с ранним return; затем let Ok(n) = ... else")
}

/// Сумма первых двух элементов; меньше двух -> 0.
pub fn sum_first_two(xs: &[i32]) -> i32 {
    todo!("let else с паттерном на xs.get(0..2) - или срезовым паттерном, как удобнее")
}
