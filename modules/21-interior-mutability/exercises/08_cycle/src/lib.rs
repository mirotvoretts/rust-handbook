//! 08 (3x) - двусторонняя связь без утечки.
//!
//! Два узла ссылаются друг на друга. Если обе ссылки сделать владеющими, получится цикл и
//! утечка: счётчики никогда не дойдут до нуля. Задача - связать узлы так, чтобы память
//! корректно освобождалась: одно направление владеет, другое - только ссылается. Типы
//! полей Node подсказывают, где какое направление. Один из тестов проверяет счётчики
//! ссылок, другой - что после удаления первого узла обратная ссылка "гаснет".

use std::cell::RefCell;
use std::rc::{Rc, Weak};

/// Узел двусвязной пары. По типам полей видно, какая ссылка владеющая, а какая - нет.
pub struct Node {
    pub value: i32,
    pub next: RefCell<Option<Rc<Node>>>,
    pub prev: RefCell<Option<Weak<Node>>>,
}

/// Одиночный узел без соседей.
pub fn node(value: i32) -> Rc<Node> {
    todo!()
}

/// Свяжи два новых узла в пару a -> b: у a появляется сосед b впереди, у b - сосед a
/// позади. Направление a -> b владеющее, b -> a - нет. Верни (a, b).
pub fn pair(a: i32, b: i32) -> (Rc<Node>, Rc<Node>) {
    todo!()
}

/// Значение следующего узла (по ссылке next), если он есть.
pub fn next_value(n: &Rc<Node>) -> Option<i32> {
    todo!()
}

/// Значение предыдущего узла (по ссылке prev), если он ещё жив.
pub fn prev_value(n: &Rc<Node>) -> Option<i32> {
    todo!()
}
