//! 07 (3x) - наблюдатели, которых источник не удерживает.
//!
//! Subject подписывает наблюдателей, но не должен продлевать им жизнь: если наблюдатель
//! уничтожен снаружи, источник обязан это заметить и пропустить его при рассылке. Выберите
//! тип хранимой ссылки так, чтобы подписка не мешала наблюдателю быть уничтоженным, а при
//! рассылке проверяйте, жив ли ещё каждый подписчик. Так избегается и утечка, и цикл
//! subject <-> observer.

use std::cell::RefCell;
use std::rc::{Rc, Weak};

/// Наблюдатель - разделяемая изменяемая ячейка с накопленным значением.
pub type Observer = Rc<RefCell<i32>>;

/// Источник событий.
#[derive(Default)]
pub struct Subject {
    observers: Vec<Weak<RefCell<i32>>>,
}

impl Subject {
    /// Пустой источник.
    pub fn new() -> Self {
        todo!()
    }

    /// Подпиши наблюдателя так, чтобы подписка не удерживала его жизнь.
    pub fn subscribe(&mut self, o: &Observer) {
        todo!()
    }

    /// Прибавь delta каждому ЖИВОМУ наблюдателю и верни число уведомлённых. Уже
    /// уничтоженных подписчиков пропусти.
    pub fn notify(&mut self, delta: i32) -> usize {
        todo!()
    }
}
