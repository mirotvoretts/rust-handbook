//! 02 (0x) - RefCell<T>: мутация внутренних данных через & с проверкой в рантайме.
//!
//! В отличие от Cell, RefCell выдаёт настоящие ссылки внутрь: одну на чтение или одну
//! на запись за раз. Правило shared-XOR-mutable здесь проверяется во время выполнения,
//! а не компилятором. Все три функции получают только &RefCell.

use std::cell::RefCell;

/// Добавь x в конец вектора.
pub fn push(v: &RefCell<Vec<i32>>, x: i32) {
    todo!()
}

/// Верни текущую длину вектора.
pub fn len(v: &RefCell<Vec<i32>>) -> usize {
    todo!()
}

/// Удвой каждый элемент вектора на месте.
pub fn double_all(v: &RefCell<Vec<i32>>) {
    todo!()
}
