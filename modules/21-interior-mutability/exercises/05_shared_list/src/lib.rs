//! 05 (2x) - список Rc<RefCell<Node>>: общий изменяемый узел.
//!
//! Каждый узел - Rc<RefCell<Node>>: на один узел может указывать несколько ручек, и он
//! изменяем через любую из них. Правка узла через одну ручку видна через все прочие,
//! ссылающиеся на тот же Rc (это проверяет один из тестов). Так строятся связанные
//! структуры с разделяемыми узлами.

use std::cell::RefCell;
use std::rc::Rc;

/// Ссылка на следующий узел (либо его нет).
pub type Link = Option<Rc<RefCell<Node>>>;

/// Узел односвязного списка.
pub struct Node {
    pub value: i32,
    pub next: Link,
}

/// Собери список из среза: xs[0] - голова, далее по next. Пустой срез -> None.
/// Верни ссылку на голову.
pub fn from_slice(xs: &[i32]) -> Link {
    todo!()
}

/// Верни сумму значений всех узлов списка.
pub fn sum(head: &Link) -> i64 {
    todo!()
}

/// Прибавь delta к значению КАЖДОГО узла на месте.
pub fn add_all(head: &Link, delta: i32) {
    todo!()
}
