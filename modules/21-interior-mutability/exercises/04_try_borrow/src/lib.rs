//! 04 (1x) - конфликт заёма без паники.
//!
//! borrow()/borrow_mut() при нарушении shared-XOR-mutable ПАНИКУЮТ. У RefCell есть
//! парные методы, которые вместо паники возвращают Result и позволяют опросить, свободен
//! ли сейчас заём. Найдите их и постройте на них функции ниже - паниковать нельзя даже
//! тогда, когда значение уже занято чужим заёмом.

use std::cell::RefCell;

/// Верни Some(значение), если прямо сейчас чтение допустимо (нет активного &mut заёма),
/// иначе None. Паниковать нельзя.
pub fn read_if_free(c: &RefCell<i32>) -> Option<i32> {
    todo!()
}

/// Прибавь delta и верни Ok(новое значение). Если значение сейчас занято заёмом и
/// изменить его нельзя - верни Err(()) вместо паники.
#[allow(clippy::result_unit_err)]
pub fn add_checked(c: &RefCell<i32>, delta: i32) -> Result<i32, ()> {
    todo!()
}
