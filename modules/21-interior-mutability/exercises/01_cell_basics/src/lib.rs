//! 01 (0x) - Cell<T>: замена значения целиком без заёмов.
//!
//! Cell не выдаёт ссылок внутрь - только меняет значение полностью. Поэтому мутация
//! идёт через &Cell, а конфликта заёмов быть не может. Загляните в документацию Cell:
//! какие методы читают, какие заменяют, какой требует T: Copy.

use std::cell::Cell;

/// Увеличь хранимое число на 1, имея на руках только &Cell.
pub fn bump(c: &Cell<i32>) {
    todo!()
}

/// Замени значение на new и верни то, что лежало раньше.
pub fn replace_get(c: &Cell<i32>, new: i32) -> i32 {
    todo!()
}

/// Забери значение наружу, оставив внутри Cell значение по умолчанию (0 для i32).
pub fn take(c: &Cell<i32>) -> i32 {
    todo!()
}
