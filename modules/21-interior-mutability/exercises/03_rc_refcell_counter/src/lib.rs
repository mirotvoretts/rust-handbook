//! 03 (1x) - Rc<RefCell<T>>: разделяемое владение + мутация в одном потоке.
//!
//! Rc даёт несколько владельцев одного значения (но лишь доступ по &T), RefCell даёт
//! мутацию через &T. Композиция - несколько ручек на ОДНО изменяемое значение: правка
//! через любую ручку видна через все остальные. Подумайте, чем "ещё одна ручка на то же
//! значение" отличается от "новое значение".

use std::cell::RefCell;
use std::rc::Rc;

/// Разделяемый счётчик.
pub type Counter = Rc<RefCell<i32>>;

/// Создай новый счётчик со значением 0.
pub fn new_counter() -> Counter {
    todo!()
}

/// Верни ещё одну ручку на ТОТ ЖЕ счётчик (а не независимую копию значения).
pub fn handle(c: &Counter) -> Counter {
    todo!()
}

/// Прибавь 1 к общему значению.
pub fn inc(c: &Counter) {
    todo!()
}

/// Верни текущее значение.
pub fn value(c: &Counter) -> i32 {
    todo!()
}
