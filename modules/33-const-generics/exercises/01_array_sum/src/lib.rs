//! 01 (0x) - функции, обобщённые по длине массива.
//!
//! Реализуй две функции, обобщённые по длине массива через const-параметр `N`:
//! - `sum<const N: usize>(arr: [i32; N]) -> i32` - сумма элементов;
//! - `dot<const N: usize>(a: [i32; N], b: [i32; N]) -> i32` - скалярное
//!   произведение (сумма поэлементных произведений).
//!
//! Подсказки:
//! - массив `[i32; N]` можно перебрать циклом `for x in arr`;
//! - для `dot` удобно идти по индексам `0..N` либо парами через `zip`.
//!
//! Заглушки помечены `todo!()` - крейт компилируется, тесты падают, пока не решено.
//! Про const-generics:
//! <https://doc.rust-lang.org/reference/items/generics.html#const-generics>.
pub fn sum<const N: usize>(_arr: [i32; N]) -> i32 {
    todo!()
}

pub fn dot<const N: usize>(_a: [i32; N], _b: [i32; N]) -> i32 {
    todo!()
}
