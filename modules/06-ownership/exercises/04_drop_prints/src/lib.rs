//! 04 (0x) — `Drop` и порядок уничтожения.
//!
//! `Noisy` при уничтожении должен записать свой `id` в общий журнал `log`. Так мы сможем
//! наблюдать, В КАКОМ ПОРЯДКЕ Rust роняет значения.
//!
//! Реализуйте `Noisy::new` и трейт `Drop` для `Noisy`.
//!
//! Напоминание про порядок: локальные переменные роняются в ОБРАТНОМ порядке объявления
//! (как стек), а элементы `Vec` — в прямом. Тесты это проверяют.

use std::cell::RefCell;
use std::rc::Rc;

pub struct Noisy {
    id: u32,
    log: Rc<RefCell<Vec<u32>>>,
}

impl Noisy {
    /// Создаёт `Noisy` с данным `id`, привязанный к общему журналу `log`.
    pub fn new(id: u32, log: Rc<RefCell<Vec<u32>>>) -> Noisy {
        todo!()
    }
}

// TODO: реализуйте `impl Drop for Noisy`, чтобы в `drop` дописывать `self.id` в конец журнала.
