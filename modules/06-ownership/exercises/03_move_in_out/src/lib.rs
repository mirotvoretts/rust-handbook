//! 03 (0x) - Владение через функции: взять и вернуть.
//!
//! Все функции забирают аргумент во владение, что-то с ним делают и возвращают владение
//! наружу. Так значение "проходит сквозь" функцию, не копируясь. Замените `todo!()`.

/// Забирает имя во владение и возвращает приветствие `"Привет, <name>!"`.
pub fn make_greeting(name: String) -> String {
    todo!()
}

/// Забирает строку, дописывает к ней `"!"` и возвращает её же (без лишних аллокаций сверх push).
pub fn append_bang(s: String) -> String {
    todo!()
}

/// Забирает вектор во владение. Возвращает пару: первый элемент (если был) и остаток вектора.
/// Порядок остальных элементов сохраняется.
pub fn split_first(v: Vec<i32>) -> (Option<i32>, Vec<i32>) {
    todo!()
}
