//! 11 (2x) — Обмен владением без клонов.
//!
//! `std::mem::swap` меняет местами содержимое двух `&mut`-ссылок, не копируя данные на куче:
//! переставляются только «внутренности» на стеке. Это позволяет перемещать владеющие значения
//! между слотами, обходя запрет на move из-под `&mut`.
//!
//! Реализуйте функции через `mem::swap` (без `.clone()`).

use std::mem;

/// Меняет местами содержимое `a` и `b`.
pub fn swap_two(a: &mut String, b: &mut String) {
    todo!()
}

/// Циклический сдвиг трёх значений: старое `a`→`b`, `b`→`c`, `c`→`a`.
/// То есть после вызова: `a` содержит бывшее `c`, `b` — бывшее `a`, `c` — бывшее `b`.
pub fn rotate_left3<T>(a: &mut T, b: &mut T, c: &mut T) {
    todo!()
}
