//! 05 (1x) - Когда нужен `.clone()`, а когда хватает move.
//!
//! `.clone()` - это НАСТОЯЩАЯ глубокая копия (новый буфер на куче). Он нужен, только если
//! оба владельца обязаны существовать одновременно. Если исходное значение больше не нужно -
//! клонировать расточительно, достаточно move.
//!
//! Реализуйте функции по описанию. `keep_both` обязан склонировать, `first_word_len` - нет.

/// Нужно вернуть ДВА независимых владельца одной и той же строки.
/// Здесь клонирование неизбежно: мы отдаём наружу два значения.
pub fn keep_both(s: String) -> (String, String) {
    todo!()
}

/// Возвращает длину первого слова (до первого пробела) и саму строку целиком.
/// Клонировать НЕ нужно: длину считаем по ссылке, а строку возвращаем через move.
pub fn first_word_len(s: String) -> (usize, String) {
    todo!()
}
