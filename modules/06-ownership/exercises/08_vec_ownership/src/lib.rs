//! 08 (1x) - Владение и `Vec`: потребить или одолжить.
//!
//! `into_iter()` ПОТРЕБЛЯЕТ вектор (владение элементами уходит в итератор), после чего
//! исходный вектор недоступен. `iter()` лишь ЗАИМСТВУЕТ (`&T`), вектор остаётся у владельца.
//! Выбор между ними - выбор между move и borrow.
//!
//! Реализуйте функции строго по сигнатурам (обратите внимание, где `Vec<T>`, а где `&[T]`).

/// Потребляет вектор и возвращает сумму его элементов. Используйте `into_iter`.
pub fn sum_owned(v: Vec<i32>) -> i32 {
    todo!()
}

/// Заимствует срез и возвращает сумму. Вектор вызывающего остаётся доступным. Используйте `iter`.
pub fn sum_borrowed(v: &[i32]) -> i32 {
    todo!()
}

/// Заимствует срез и возвращает НОВЫЙ вектор, где каждый элемент удвоен.
pub fn doubled(v: &[i32]) -> Vec<i32> {
    todo!()
}

/// Потребляет вектор строк и склеивает их в одну строку через разделитель `sep`.
/// Раз вектор нам отдали во владение, лишних клонов строк быть не должно.
pub fn join_owned(v: Vec<String>, sep: &str) -> String {
    todo!()
}
