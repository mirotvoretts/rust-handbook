//! 01 (0x) - Основы move-семантики.
//!
//! Здесь всё передаётся ПО ЗНАЧЕНИЮ (владение уходит в функцию). Реализуйте функции так,
//! чтобы тесты прошли. Обратите внимание: после того как значение передано в функцию,
//! исходная переменная становится moved-from - это и есть move-семантика.
//!
//! Замените каждый `todo!()` на реализацию.

/// Склеивает две строки в одну (владение обеими уходит внутрь и потребляется).
pub fn concat(a: String, b: String) -> String {
    todo!("верните a, к которой присоединена b")
}

/// Забирает вектор строк во владение и возвращает суммарную длину всех строк.
pub fn total_len(items: Vec<String>) -> usize {
    todo!("просуммируйте длины всех строк в items")
}

/// Забирает строку во владение и возвращает её же, но в верхнем регистре.
pub fn shout(s: String) -> String {
    todo!("верните s в верхнем регистре")
}
