//! 09 (2x) — `while let`: вычерпывание `Vec`.
//!
//! `Vec::pop` возвращает `Option`. `while let Some(x) = v.pop()` крутится, пока вектор не пуст —
//! компактная идиома «обработать и опустошить».

/// Снимает все элементы стека и возвращает их в порядке снятия (LIFO).
/// Для [1, 2, 3] результат [3, 2, 1], а сам стек становится пустым.
pub fn pop_all(stack: &mut Vec<i32>) -> Vec<i32> {
    todo!("цикл while let Some(x) = stack.pop(), проталкивая x в out")
}

/// Суммирует и опустошает стек, снимая элементы через `while let`.
pub fn sum_drain(stack: &mut Vec<i32>) -> i32 {
    todo!()
}
