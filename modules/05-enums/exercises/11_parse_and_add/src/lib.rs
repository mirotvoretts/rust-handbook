//! 11 (2x) - Проброска ошибки вручную через `match`.
//!
//! Строку в число превращает `str::parse` (возвращает `Result`). Разберите оба разбора и
//! верните первую ошибку, либо сумму. Оператор `?` появится позже (M16) - здесь делаем руками.

/// Разбирает два числа и возвращает их сумму. При неудаче разбора любого аргумента -
/// `Err("bad number: <arg>")` для первого сбойного (сначала проверяется `a`).
pub fn parse_and_add(a: &str, b: &str) -> Result<i32, String> {
    todo!("match a.parse::<i32>() ... затем b, вернуть Ok(x + y)")
}
