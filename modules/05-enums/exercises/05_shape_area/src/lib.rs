//! 05 (1x) - Enum с данными и деструктуризация в `match`.
//!
//! Каждый вариант `Shape` несёт свои данные (кортежные или структурные). `match` извлекает их,
//! связывая с именами.

pub enum Shape {
    Circle(f64),                         // радиус
    Rectangle(f64, f64),                 // ширина, высота
    Triangle { base: f64, height: f64 }, // основание, высота
}

/// Площадь фигуры. Разберите `shape` по вариантам, извлекая поля.
/// Круг: π·r²; прямоугольник: w·h; треугольник: ½·base·height.
pub fn area(shape: &Shape) -> f64 {
    todo!("match shape по трём вариантам, используя std::f64::consts::PI")
}
