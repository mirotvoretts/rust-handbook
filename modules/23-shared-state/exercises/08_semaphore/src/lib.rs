//! 08 (3x) - counting semaphore на Mutex + Condvar.
//!
//! Семафор с `permits` разрешениями ограничивает число потоков, одновременно
//! работающих в критической зоне. acquire() блокирует поток, пока есть хотя бы одно
//! свободное разрешение, затем забирает одно. release() возвращает разрешение и
//! будит одного ждущего. Инвариант: между парными acquire/release одновременно
//! проходят не более `permits` потоков (при permits == 1 семафор вырождается в
//! мьютекс).
//!
//! Реализуй через Mutex<usize> (число свободных разрешений) + Condvar. acquire ждёт,
//! пока разрешений станет больше нуля, проверяя условие в цикле while (ложные
//! пробуждения); release увеличивает счётчик и вызывает notify_one. Busy-wait
//! (крутить acquire в пустом цикле, опрашивая счётчик) не годится - потоки должны
//! спать на Condvar, а не жечь процессор.

use std::sync::{Condvar, Mutex};

pub struct Semaphore {
    // TODO: Mutex<usize> (свободные разрешения) + Condvar.
}

impl Semaphore {
    /// Семафор с `permits` начальными разрешениями.
    pub fn new(permits: usize) -> Self {
        todo!()
    }

    /// Забрать одно разрешение, блокируясь, пока свободных нет.
    pub fn acquire(&self) {
        todo!()
    }

    /// Вернуть одно разрешение и разбудить одного ждущего.
    pub fn release(&self) {
        todo!()
    }
}
