//! 03 (1x) - Arc<Mutex<Vec<T>>>: параллельно наполнить общий вектор.
//!
//! Каждый элемент входа обработай в отдельном потоке (возведи в квадрат) и допиши
//! результат в общий Vec под мьютексом. После join забери вектор обратно из
//! Arc<Mutex<..>> без лишнего клонирования: Arc::try_unwrap + Mutex::into_inner.
//! Порядок вставки между потоками не определён, поэтому верни результат
//! отсортированным по возрастанию.

use std::sync::{Arc, Mutex};
use std::thread;

/// Возведи каждый элемент `input` в квадрат, распараллелив работу по потокам,
/// и верни вектор квадратов, отсортированный по возрастанию.
pub fn parallel_squares(input: Vec<i64>) -> Vec<i64> {
    todo!()
}
