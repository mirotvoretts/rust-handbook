//! 05 (2x) - барьер на Mutex + Condvar.
//!
//! Барьер на N участников: поток, вызвавший wait(), блокируется, пока wait() не
//! позовут ровно N раз; после этого все заблокированные разблокируются. Смысл -
//! точка рандеву: ни один поток не проходит барьер, пока не подтянулись все.
//!
//! Реализуй через Mutex<состояние> + Condvar. Последний пришедший будит остальных
//! через notify_all; ждущие проверяют условие в цикле while (из-за ложных
//! пробуждений - spurious wakeups - нельзя ограничиться if). Стандартный
//! std::sync::Barrier использовать нельзя - смысл упражнения собрать его руками.
//!
//! Гарантия использования в тестах: на каждый Barrier::new(n) метод wait()
//! вызывается ровно n раз (по разу из n потоков).

use std::sync::{Condvar, Mutex};

pub struct Barrier {
    // TODO: число участников n, Mutex<usize> (сколько уже пришло), Condvar.
}

impl Barrier {
    /// Барьер, рассчитанный на `n` участников.
    pub fn new(n: usize) -> Self {
        todo!()
    }

    /// Заблокировать текущий поток, пока wait() не будет вызван n раз суммарно.
    pub fn wait(&self) {
        todo!()
    }
}
