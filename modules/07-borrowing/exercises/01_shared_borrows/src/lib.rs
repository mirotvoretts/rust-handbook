//! 01 (0x) — Много общих заёмов `&` одновременно.
//!
//! Общих ссылок `&T` может быть сколько угодно: чтение не мешает чтению. `summary` берёт два
//! заёма одного вектора сразу — это законно.

/// Сумма всех элементов (берёт вектор по общей ссылке).
pub fn total(xs: &Vec<i32>) -> i32 {
    todo!()
}

/// Сколько элементов строго положительны.
pub fn count_positive(xs: &Vec<i32>) -> usize {
    todo!()
}

/// Пара (сумма, число положительных). Вызовите обе функции выше — два общих заёма сосуществуют.
pub fn summary(xs: &Vec<i32>) -> (i32, usize) {
    todo!("(total(xs), count_positive(xs))")
}
