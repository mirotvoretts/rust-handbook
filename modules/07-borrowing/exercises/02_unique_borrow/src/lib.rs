//! 02 (0x) — Изменение через единственный `&mut`.
//!
//! Изменяемый заём `&mut T` — единственный, зато позволяет менять значение. Функции берут
//! вектор по `&mut` и меняют его на месте.

/// Добавляет в конец удвоенное значение.
pub fn push_doubled(xs: &mut Vec<i32>, value: i32) {
    todo!("xs.push(value * 2)")
}

/// Умножает каждый элемент на `k`.
pub fn scale_all(xs: &mut Vec<i32>, k: i32) {
    todo!()
}
