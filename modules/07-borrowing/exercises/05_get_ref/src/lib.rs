//! 05 (1x) — Возврат `Option<&T>`.
//!
//! Когда ссылка на элемент может отсутствовать, тип — `Option<&T>`. Это заём: результат живёт
//! не дольше входного среза.

/// Ссылка на элемент по индексу, `None` при выходе за границы. Подсказка: `xs.get(i)`.
pub fn get(xs: &[i32], i: usize) -> Option<&i32> {
    todo!()
}

/// Ссылка на первый элемент, равный `target`, либо `None`.
pub fn find(xs: &[i32], target: i32) -> Option<&i32> {
    todo!("пройдитесь по xs и верните &x при совпадении")
}
