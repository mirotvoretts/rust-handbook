//! 10 (2x) - Заём среза, владеющий результат.
//!
//! Функция читает срез (общий заём) и возвращает НОВОЕ владеющее значение (`Option<i32>`) -
//! заём при этом не "утекает" наружу.

/// Максимум среди сумм соседних пар: для [1,2,3,4] пары дают 3,5,7 -> Some(7).
/// Для срезов длины < 2 - `None`.
pub fn max_pair_sum(xs: &[i32]) -> Option<i32> {
    todo!("пройдитесь по соседним парам xs[i-1]+xs[i], найдите максимум")
}
