//! 04 (0x) — Возврат заимствованного среза.
//!
//! Функция может вернуть `&str`/`&[T]`, заимствованный ИЗ своего аргумента. Компилятор
//! автоматически связывает время жизни результата со входом (подробно — M08).

/// Первое слово строки (до первого пробела); если пробела нет — вся строка.
/// Возвращается СРЕЗ входной строки, без копирования.
pub fn first_word(s: &str) -> &str {
    todo!("найдите индекс первого пробела и верните срез s[..i], иначе s целиком")
}

/// Все элементы, кроме первого (для пустого/одноэлементного — пустой срез).
pub fn tail(xs: &[i32]) -> &[i32] {
    todo!()
}
