//! 07 (1x) - Изменение `&mut [i32]` на месте.
//!
//! Изменяемый заём среза - единственный, поэтому внутри функции можно свободно менять элементы.

/// Меняет местами первый и последний элементы (для длины < 2 ничего не делает).
pub fn swap_ends(xs: &mut [i32]) {
    todo!("если длина >= 2, xs.swap(0, len - 1)")
}

/// Меняет знак каждого элемента.
pub fn negate_all(xs: &mut [i32]) {
    todo!()
}
