//! 09 (2x) — Найти индекс максимума, затем обнулить его.
//!
//! Снова две фазы: сначала вычисляем индекс первого максимума (только чтение), затем меняем
//! элемент по этому индексу. Индекс — это `usize`, он «переживает» заём, а сам заём завершается.

/// Находит индекс ПЕРВОГО максимального элемента и записывает в него 0.
/// [1, 9, 3, 9] -> [1, 0, 3, 9]. Пустой срез не меняется.
pub fn zero_out_max(xs: &mut [i32]) {
    todo!("найдите индекс максимума, затем xs[idx] = 0")
}
