//! 03 (0x) - Прочитать через `&`, затем поменять через `&mut` (NLL).
//!
//! Заём живёт до последнего использования. Сначала посчитайте значение (общий заём внутри
//! вектора завершится), затем измените вектор - конфликта нет.

/// Добавляет в конец сумму текущих элементов.
/// Например [1, 2, 3] -> [1, 2, 3, 6].
pub fn push_sum(xs: &mut Vec<i32>) {
    todo!("сначала посчитайте сумму, затем xs.push(sum)")
}

/// Добавляет в конец текущую длину вектора (как i32).
/// Например [5, 5] -> [5, 5, 2].
pub fn push_len(xs: &mut Vec<i32>) {
    todo!()
}
