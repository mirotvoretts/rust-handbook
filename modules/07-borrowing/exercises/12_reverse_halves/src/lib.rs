//! 12 (3x) — Два непересекающихся `&mut` через `split_at_mut`.
//!
//! Наивно взять `(&mut xs[..k], &mut xs[k..])` компилятор не разрешит — он не может доказать
//! непересечение по индексам. Для этого есть `split_at_mut(k)`, безопасно дающий ДВА
//! изменяемых среза в непересекающиеся половины одновременно.
//!
//! Разверните каждую половину НЕЗАВИСИМО. Делите по `len / 2`: левая половина — первые
//! `len/2` элементов, правая — остальные.
//!   [1,2,3,4]   -> [2,1, 4,3]
//!   [1,2,3,4,5] -> [2,1, 5,4,3]

pub fn reverse_halves(xs: &mut [i32]) {
    todo!("let (left, right) = xs.split_at_mut(xs.len() / 2); разверните каждую половину")
}
