//! 12 (3x) - Коллатц через выражения, без `return`.
//!
//! Соберите обе функции, комбинируя `while`, `if`-ВЫРАЖЕНИЕ и изменяемое состояние.
//! Не используйте `return`: результат - последнее выражение тела.
//!
//! Последовательность Коллатца из `start`: чётное -> n/2, нечётное -> 3n+1, до 1 включительно.

/// Максимальное значение, встречающееся в последовательности (включая сам `start` и 1).
/// collatz_max(1)=1, collatz_max(6)=16, collatz_max(7)=52.
pub fn collatz_max(start: u64) -> u64 {
    todo!("держите текущее n и максимум; обновляйте максимум на каждом шаге")
}

/// Число членов последовательности, включая `start` и завершающую 1.
/// collatz_len(1)=1, collatz_len(6)=9, collatz_len(7)=17.
pub fn collatz_len(start: u64) -> u64 {
    todo!("считайте члены, начиная с 1 (сам start)")
}
