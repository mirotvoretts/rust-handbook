//! 08 (1x) - FizzBuzz: `for` + цепочка `else if`.
//!
//! Для чисел 1..=n соберите строки: кратно 15 -> "FizzBuzz", кратно 3 -> "Fizz",
//! кратно 5 -> "Buzz", иначе - само число как строка. ВАЖЕН порядок проверок:
//! делимость на 15 нужно проверять раньше, чем на 3 и 5.
//!
//! Подсказка: строку из числа даёт `i.to_string()`, добавить в вектор - `out.push(...)`.

/// Возвращает вектор из n строк FizzBuzz для чисел от 1 до n включительно.
pub fn fizzbuzz(n: u32) -> Vec<String> {
    todo!("создайте вектор, наполните его в цикле for i in 1..=n, верните его")
}
