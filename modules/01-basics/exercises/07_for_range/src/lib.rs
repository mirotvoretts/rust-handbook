//! 07 (1x) - `for` по диапазону.
//!
//! `for i in 0..n` перебирает 0..n-1; `1..=n` включает конец. Это идиоматичная замена
//! C-циклу `for (i = 0; i < n; i++)`.

/// Факториал n! = 1 * 2 * ... * n. factorial(0) == 1.
pub fn factorial(n: u64) -> u64 {
    todo!("перемножьте все i от 1 до n включительно")
}

/// Сумма всех натуральных чисел СТРОГО МЕНЬШЕ `limit`, кратных 3 или 5
/// (классическая задача Project Euler #1). Для limit == 10 это 3+5+6+9 = 23.
pub fn sum_multiples_below(limit: u32) -> u32 {
    todo!("суммируйте i, кратные 3 или 5, для i от 1 до limit-1")
}
