//! 01 (0x) — Привязки `let`/`mut`.
//!
//! Привязки в Rust неизменяемы по умолчанию. Там, где значение нужно менять (накапливать),
//! используйте `let mut`. Замените каждый `todo!()` на реализацию.

/// Прибавляет 1 к `start` ровно три раза и возвращает результат.
/// Заведите ИЗМЕНЯЕМУЮ переменную и увеличьте её тремя отдельными шагами (без цикла).
pub fn increment_thrice(start: i32) -> i32 {
    todo!("let mut acc = start; затем три раза acc += 1")
}

/// Возвращает сумму трёх чисел, накапливая её в изменяемой переменной
/// (тремя шагами `+=`, а не одним выражением `a + b + c`).
pub fn running_total(a: i32, b: i32, c: i32) -> i32 {
    todo!("let mut t = 0; t += a; t += b; t += c; t")
}

/// Возвращает пару с переставленными местами значениями: `(a, b) -> (b, a)`.
pub fn swapped(a: i32, b: i32) -> (i32, i32) {
    todo!()
}
