//! 10 (2x) — Цикл с изменяемым состоянием.
//!
//! Многие алгоритмы — это «крутим цикл, пока держим и обновляем переменные-состояние».

/// Целочисленный квадратный корень: наибольшее `r`, такое что `r * r <= n`.
/// int_sqrt(0)=0, int_sqrt(15)=3, int_sqrt(16)=4, int_sqrt(24)=4, int_sqrt(25)=5.
pub fn int_sqrt(n: u64) -> u64 {
    todo!("наращивайте r, пока (r+1)*(r+1) <= n")
}

/// Разворачивает десятичные цифры числа: 123 -> 321, 100 -> 1, 0 -> 0.
pub fn reverse_digits(n: u32) -> u32 {
    todo!("накапливайте rev = rev*10 + последняя цифра, отбрасывая цифры делением на 10")
}
