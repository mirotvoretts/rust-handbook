//! 04 (0x) - `if` как выражение.
//!
//! В Rust `if`/`else if`/`else` - это ВЫРАЖЕНИЕ: его можно вернуть напрямую. Все ветки
//! обязаны иметь один тип, а условие - быть `bool`. Реализуйте без `return`.

/// Знак числа: -1 для отрицательных, 0 для нуля, 1 для положительных.
pub fn sign(n: i32) -> i32 {
    todo!("верните -1 при n<0, 1 при n>0, иначе 0 - через if / else if")
}

/// Буквенная оценка по баллу: >=90 'A', >=80 'B', >=70 'C', >=60 'D', иначе 'F'.
pub fn grade(score: u32) -> char {
    todo!("цепочка else if, возвращающая char")
}

/// Абсолютная разность двух чисел, через `if`-выражение (без функции `abs`).
pub fn abs_diff(a: i32, b: i32) -> i32 {
    todo!()
}
