//! 09 (2x) — Метки циклов и `break 'label`.
//!
//! По умолчанию `break` выходит из ближайшего цикла. Чтобы выйти из ВНЕШНЕГО из вложенного,
//! помечайте цикл меткой (`'outer:`) и пишите `break 'outer`. Так же можно вернуть значение:
//! `break 'outer value`.

/// Ищет разложение `target = i * j` с 2 <= i <= j. Возвращает первую такую пару (при переборе
/// i по возрастанию), либо `None`, если число простое или меньше 4.
/// Пример: 12 -> Some((2, 6)); 9 -> Some((3, 3)); 7 -> None; 1 -> None.
pub fn factor_pair(target: u32) -> Option<(u32, u32)> {
    todo!("вложенные for по i и j, break 'outer при i*j == target")
}

/// Пифагорова тройка (a, b, c) с a < b < c, a + b + c == perimeter и a² + b² == c².
/// Возвращает первую найденную по возрастанию a, затем b, либо `None`.
/// Пример: 12 -> Some((3, 4, 5)); 1000 -> Some((200, 375, 425)); 4 -> None.
pub fn pythagorean_triple(perimeter: u32) -> Option<(u32, u32, u32)> {
    todo!("вложенные for по a и b; c = perimeter - a - b; break 'outer при совпадении")
}
