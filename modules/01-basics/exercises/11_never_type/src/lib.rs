//! 11 (2x) — Тип `!` (never) в позиции значения.
//!
//! Выражения `panic!(...)` и `unreachable!()` имеют тип `!`, который приводится к ЛЮБОМУ типу.
//! Поэтому такую расходящуюся ветку можно поставить туда, где ожидается конкретный тип, не
//! ломая правило «все ветки одного типа». Реализуйте функции — на корректных входах они
//! возвращают значение, на некорректных расходятся (паникуют).

/// Разбирает символ-бит в число: '0' -> 0, '1' -> 1. Любой другой символ недопустим —
/// в этой ветке используйте `unreachable!()` (её тип `!` подстроится под `u8`).
pub fn parse_bit(c: char) -> u8 {
    todo!("match по c: '0' в 0, '1' в 1, иначе unreachable!()")
}

/// Возвращает значение из `Option`, а на `None` — паникует (ветка типа `!`).
pub fn get_or_panic(opt: Option<i32>) -> i32 {
    todo!("match по opt: Some в значение, None в panic!")
}

/// Делит `n` на `d`. При `d == 0` вместо возврата значения ветка `else` расходится через
/// `panic!` — обратите внимание, что `if`-выражение всё равно имеет тип `i32`.
pub fn checked_div(n: i32, d: i32) -> i32 {
    todo!("при d == 0 расходитесь через panic!, иначе верните n / d")
}
