//! 03 (0x) — Блок как выражение.
//!
//! Тело функции и любой блок `{ ... }` — это ВЫРАЖЕНИЕ: его значение равно последнему
//! выражению БЕЗ точки с запятой. Реализуйте функции БЕЗ ключевого слова `return` и без
//! «висячей» `;` в конце (иначе тип станет `()` и тесты не скомпилируются).

/// Квадрат гипотенузы: a² + b². Внутри заведите промежуточные привязки и верните их сумму
/// как значение блока.
pub fn hypot_sq(a: i32, b: i32) -> i32 {
    todo!("заведите a2 и b2, верните их сумму как последнее выражение блока")
}

/// Перевод градусов Цельсия в Фаренгейты по формуле c * 9 / 5 + 32 (целочисленно).
pub fn celsius_to_fahrenheit(c: i32) -> i32 {
    todo!()
}

/// Среднее арифметическое трёх чисел (целочисленное деление).
pub fn average3(a: i32, b: i32, c: i32) -> i32 {
    todo!()
}
