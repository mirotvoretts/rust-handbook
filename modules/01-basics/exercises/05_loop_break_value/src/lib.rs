//! 05 (1x) — `loop` с возвратом значения.
//!
//! `loop { ... }` — единственный цикл-выражение: `break value;` делает `value` результатом
//! всего `loop`. Используйте этот приём.

/// Наименьшая степень двойки (1, 2, 4, 8, ...), которая не меньше `n`.
/// Заведите переменную `= 1` и удваивайте в `loop`, пока не достигнете `>= n`,
/// затем `break` этим значением.
pub fn first_pow2_at_least(n: u32) -> u32 {
    todo!("удваивайте p в loop; когда p >= n, сделайте break p")
}

/// Сумма 1 + 2 + ... + n, посчитанная в `loop` со счётчиком (для n == 0 результат 0).
pub fn sum_1_to(n: u32) -> u32 {
    todo!()
}
