//! 06 (1x) - Цикл `while`.
//!
//! `while условие { ... }` крутится, пока условие истинно. Значения не возвращает -
//! накапливайте результат в изменяемой переменной.

/// Наибольший общий делитель по алгоритму Евклида (через остаток), реализованный на `while`.
/// gcd(a, 0) == a, gcd(0, b) == b.
pub fn gcd(a: u64, b: u64) -> u64 {
    todo!("пока b != 0: остаток a % b, затем сдвиг a = b, b = остаток")
}

/// Длина последовательности Коллатца: сколько шагов нужно, чтобы из `n` прийти к 1.
/// Шаг: чётное -> n/2, нечётное -> 3n+1. collatz_steps(1) == 0.
pub fn collatz_steps(n: u64) -> u64 {
    todo!("считайте шаги, пока n != 1")
}
