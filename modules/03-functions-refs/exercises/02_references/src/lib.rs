//! 02 (0x) - Ссылки `&T`: заём для чтения.
//!
//! Взяв `&T`, функция читает значение, НЕ забирая владения - вызывающий сохраняет доступ.
//! Для арифметики над ссылкой на число нужно разыменование `*`.

/// Сумма двух чисел, переданных по ссылке.
pub fn deref_sum(a: &i32, b: &i32) -> i32 {
    todo!("*a + *b")
}

/// Большее из двух чисел, переданных по ссылке.
pub fn max_ref(a: &i32, b: &i32) -> i32 {
    todo!()
}

/// Длина строки, переданной по ссылке (вызывающий сохранит владение своей `String`).
pub fn len_of(s: &String) -> usize {
    todo!()
}
