//! 01 (0x) - Объявление функций, возврат, ранний `return`.
//!
//! Типы параметров и возврата указываются явно. Тело - блок-выражение (последнее выражение
//! без `;` - результат); для досрочного выхода есть `return`.

/// Квадрат числа.
pub fn square(n: i32) -> i32 {
    todo!()
}

/// Чётно ли число.
pub fn is_even(n: i32) -> bool {
    todo!()
}

/// Модуль числа. Используйте ранний `return` для отрицательного случая (без функции `abs`).
pub fn abs_val(n: i32) -> i32 {
    todo!("if n < 0 верните -n досрочно, иначе n")
}
