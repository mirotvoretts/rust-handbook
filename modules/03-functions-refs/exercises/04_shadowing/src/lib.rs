//! 04 (0x) — Shadowing (затенение).
//!
//! Повторный `let` с тем же именем создаёт НОВОЕ связывание, затеняющее старое, и может менять
//! тип. Реализуйте функции, применяя затенение.

/// Возвращает ((n + 5) * 2) - 1, вычисляя пошагово через ЗАТЕНЕНИЕ имени `n`
/// (`let n = n + 5;` и т.д., без `mut`).
pub fn shadow_math(n: i32) -> i32 {
    todo!("let n = n + 5; let n = n * 2; let n = n - 1; n")
}

/// Число символов строки после обрезки пробелов по краям. Реализуйте через затенение `s`:
/// сначала `let s = s.trim();` (тип &str), затем `let s = s.chars().count();` (тип usize).
pub fn trimmed_len(s: &str) -> usize {
    todo!()
}
