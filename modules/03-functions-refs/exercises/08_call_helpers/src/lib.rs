//! 08 (1x) — Вызов одной функции из другой.
//!
//! В Rust порядок объявления не важен: `count_primes_below` может опираться на `is_prime`,
//! объявленную рядом. Разбивайте задачу на функции.

/// Простое ли число (n < 2 — не простое).
pub fn is_prime(n: u32) -> bool {
    todo!("нет делителей от 2 до n-1")
}

/// Сколько простых чисел строго меньше `limit`. Должна ВЫЗЫВАТЬ `is_prime`.
pub fn count_primes_below(limit: u32) -> usize {
    todo!("переберите 2..limit и посчитайте те, для которых is_prime истинно")
}
