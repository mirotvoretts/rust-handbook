//! 12 (3x) — Сортировка пузырьком на месте.
//!
//! Соедините всё из модуля: изменяемый срез `&mut [i32]`, вложенные циклы, обмен соседних
//! элементов (`xs.swap(i, i + 1)`), и функцию-помощник. Сортируйте НА МЕСТЕ, не создавая
//! новый вектор и не вызывая `xs.sort()`.

/// Отсортирован ли срез по неубыванию (помощник; пустой и одноэлементный — да).
pub fn is_sorted_asc(xs: &[i32]) -> bool {
    todo!()
}

/// Сортирует срез по неубыванию на месте методом пузырька.
pub fn bubble_sort(xs: &mut [i32]) {
    todo!("проходы по срезу, меняя местами соседей, пока есть перестановки")
}
