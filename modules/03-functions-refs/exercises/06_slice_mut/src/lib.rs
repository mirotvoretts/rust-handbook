//! 06 (1x) — Изменение `&mut [i32]` на месте.
//!
//! Изменяемый срез позволяет менять элементы последовательности без создания новой. Перебор с
//! мутацией: `for x in xs.iter_mut() { *x = ... }`. Обмен элементов: `xs.swap(i, j)`.

/// Заполняет весь срез значением `value`.
pub fn fill(xs: &mut [i32], value: i32) {
    todo!("for x in xs.iter_mut() ...")
}

/// Прибавляет `delta` к каждому элементу.
pub fn add_to_each(xs: &mut [i32], delta: i32) {
    todo!()
}

/// Разворачивает срез на месте (без выделения нового вектора).
pub fn reverse_in_place(xs: &mut [i32]) {
    todo!("два индекса с краёв к центру, меняйте местами через xs.swap(i, j)")
}
