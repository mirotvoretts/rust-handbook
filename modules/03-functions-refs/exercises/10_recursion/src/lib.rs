//! 10 (2x) - Рекурсивные функции.
//!
//! Функция может вызывать саму себя. Обязательно нужен базовый случай, иначе - бесконечная
//! рекурсия и переполнение стека.

/// n-е число Фибоначчи: fib(0)=0, fib(1)=1, fib(n)=fib(n-1)+fib(n-2).
pub fn fib(n: u32) -> u64 {
    todo!("базовые случаи для 0 и 1, иначе сумма двух предыдущих")
}

/// Возведение в степень: pow(base, exp) = base^exp, pow(base, 0) = 1.
pub fn pow(base: u64, exp: u32) -> u64 {
    todo!()
}

/// Сумма десятичных цифр числа, рекурсивно: sum_digits(n) = n%10 + sum_digits(n/10).
pub fn sum_digits(n: u32) -> u32 {
    todo!("базовый случай: n < 10")
}
