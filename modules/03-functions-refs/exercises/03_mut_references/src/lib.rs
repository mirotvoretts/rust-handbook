//! 03 (0x) — Изменяемые ссылки `&mut T`.
//!
//! `&mut T` даёт функции право менять значение на месте. Пишите через разыменование `*n`.
//! Возвращать ничего не нужно — эффект виден вызывающему.

/// Увеличивает значение на 1.
pub fn increment(n: &mut i32) {
    todo!("*n += 1")
}

/// Удваивает значение.
pub fn double(n: &mut i32) {
    todo!()
}

/// Записывает в значение `value`.
pub fn set_to(n: &mut i32, value: i32) {
    todo!()
}
