//! 05 (1x) - Обмен и изменение через `&mut`.
//!
//! Классический "обмен по указателю" из C - но безопасно. Реализуйте вручную через временную
//! переменную (не через `std::mem::swap`).

/// Меняет местами значения по двум изменяемым ссылкам.
pub fn swap(a: &mut i32, b: &mut i32) {
    todo!("временная переменная: t = *a; *a = *b; *b = t;")
}

/// Прибавляет `delta` к значению по ссылке.
pub fn add_assign(target: &mut i32, delta: i32) {
    todo!()
}
