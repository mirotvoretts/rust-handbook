//! 11 (2x) - Функции, возвращающие `Option`.
//!
//! Когда результат может отсутствовать, сигнатура возвращает `Option<T>`: `Some(value)` или
//! `None`. Это типобезопасная замена "магическим" значениям вроде -1 или нулевого указателя.

/// Целочисленное деление, `None` при делении на ноль.
pub fn safe_div(a: i32, b: i32) -> Option<i32> {
    todo!()
}

/// Элемент среза по индексу, `None` при выходе за границы. Подсказка: `xs.get(i)` даёт `Option`.
pub fn nth(xs: &[i32], i: usize) -> Option<i32> {
    todo!()
}

/// Индекс первого вхождения `target`, либо `None`.
pub fn find_index(xs: &[i32], target: i32) -> Option<usize> {
    todo!()
}
