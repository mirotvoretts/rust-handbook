//! 03 (1x) - Функции высшего порядка: границы Fn / FnMut / FnOnce.
//!
//! Границы уже проставлены - каждая самая слабая из достаточных телу.
//! Твоя задача - написать тела. Подумай, ПОЧЕМУ здесь именно такая граница:
//! apply_twice зовёт f дважды по &self (Fn); for_each зовёт много раз и
//! разрешает мутировать окружение (FnMut); eval зовёт один раз и может
//! потребить захваты (FnOnce).

/// Применяет f к x дважды: apply_twice(f, x) = f(f(x)).
pub fn apply_twice<F: Fn(i32) -> i32>(f: F, x: i32) -> i32 {
    todo!("f(f(x))")
}

/// Зовёт f для каждого элемента слева направо. f может копить состояние
/// в своём окружении (напр. сумму), поэтому граница - FnMut.
pub fn for_each<F: FnMut(i32)>(xs: &[i32], mut f: F) {
    todo!("for &x in xs { f(x); }")
}

/// Ленивое значение: f вызывается РОВНО один раз, может потребить захваты.
pub fn eval<R, F: FnOnce() -> R>(f: F) -> R {
    todo!("f()")
}
