//! 01 (0x) - Синтаксис замыканий: вывод типов, захват по чтению.
//!
//! Тело каждой функции - это создание замыкания и его вызов. Аргументы
//! функции замыкание захватывает по чтению (&T). Циклы и ручные структуры
//! не нужны - только `let f = |...| ...;` и `f(...)`.

/// Замыкание БЕЗ захватов: возведи x в квадрат.
/// Создай `|n| n * n`, вызови его с x и верни результат.
pub fn square(x: i32) -> i32 {
    todo!("let f = |n| n * n; f(x)")
}

/// Захват по чтению: a и b попадают в окружение замыкания (как &i32).
/// Замыкание без аргументов возвращает их сумму - вызови и верни.
pub fn sum_via_capture(a: i32, b: i32) -> i32 {
    todo!("let f = || a + b; f()")
}

/// Захват &str по чтению: замыкание строит приветствие "Hello, {name}!".
/// Создай его, вызови, верни строку.
pub fn greet(name: &str) -> String {
    todo!("let f = || format!(\"Hello, {name}!\"); f()")
}
