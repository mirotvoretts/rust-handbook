//! 04 (1x) - Фабрики замыканий: move + impl FnMut.
//!
//! Возврат замыкания из функции требует `move`: иначе замыкание заимствует
//! локальную переменную, которая умрёт на выходе (E0373). Тип возврата -
//! `impl FnMut(...) -> ...`, потому что замыкание меняет свой захват.

/// Фабрика счётчиков. Каждый вызов возвращённого замыкания даёт следующее
/// число, начиная с 1 (1, 2, 3, ...). Разные счётчики независимы.
pub fn make_counter() -> impl FnMut() -> u32 {
    todo!("let mut count = 0; move || { count += 1; count }")
}

/// Аккумулятор. Возвращённое замыкание прибавляет аргумент к внутренней
/// сумме (начинается со start) и возвращает текущий итог.
pub fn make_adder(start: i64) -> impl FnMut(i64) -> i64 {
    todo!("let mut total = start; move |x| { total += x; total }")
}
