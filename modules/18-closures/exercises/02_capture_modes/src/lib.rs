//! 02 (0x) - Три способа захвата в одном упражнении.
//!
//! Компилятор выбирает способ захвата по телу: чтение -> &T, мутация -> &mut T,
//! потребление -> T. Реализуй каждую функцию замыканием с нужным способом.

/// Захват по &T (чтение). Замыкание читает длину среза - создай и вызови.
pub fn peek_len(v: &[i32]) -> usize {
    todo!("let f = || v.len(); f()")
}

/// Захват по &mut T (мутация). Замыкание добавляет x в вектор.
/// Позови его ДВАЖДЫ. Подсказка: привязка должна быть `let mut f = ...`.
pub fn push_twice(v: &mut Vec<i32>, x: i32) {
    todo!("let mut f = || v.push(x); f(); f();")
}

/// Захват по значению T. `move` перемещает s в замыкание (FnOnce);
/// вызови один раз и верни строку наружу.
pub fn into_owned(s: String) -> String {
    todo!("let f = move || s; f()")
}
