//! 06 (2x) - Комбинатор retry(n, f): FnMut-замыкание с состоянием попыток.
//!
//! Граница f - FnMut: между попытками замыкание меняет своё окружение
//! (счётчик попыток, ресурс). Это ровно тот случай, где FnOnce не хватило бы,
//! а Fn был бы слишком строг.

/// Пытается выполнить f, пока не получит Ok, но не более n раз (n трактуется
/// как общее число попыток; при n == 0 попытка всё равно делается одна, чтобы
/// было что вернуть). Возвращает первый Ok или последнюю Err.
pub fn retry<T, E, F>(n: u32, mut f: F) -> Result<T, E>
where
    F: FnMut() -> Result<T, E>,
{
    todo!("хотя бы один вызов f, затем повтор пока Err и остались попытки")
}
