//! 06 (1x) - Паника и тест `#[should_panic]`.
//!
//! Иногда корректное поведение функции - это паника на недопустимом входе. Тест из
//! `tests/tests.rs` проверяет обе стороны: обычный `#[test]` для валидных входов и
//! `#[should_panic]` для паники.

/// Возвращает 100 / n. При n == 0 ПАНИКУЕТ с сообщением "division by zero".
pub fn safe_div_100(n: i32) -> i32 {
    todo!("при n == 0 вызовите panic!, иначе верните 100 / n")
}
