//! 05 (1x) - Doc-тесты.
//!
//! Пример кода в `///`-комментарии ниже - это ТЕСТ: `cargo test` его выполнит. Пока функция
//! возвращает `todo!()`, doc-тест падает (паникует). Реализуйте `square`, и он позеленеет
//! вместе с обычным тестом из `tests/tests.rs`.

/// Возводит число в квадрат.
///
/// # Примеры
/// ```
/// use ex_00_05_doctest::square;
/// assert_eq!(square(4), 16);
/// assert_eq!(square(-3), 9);
/// ```
pub fn square(n: i32) -> i32 {
    todo!()
}
