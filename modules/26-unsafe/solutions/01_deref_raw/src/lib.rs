//! 01 (0x) - сырой указатель и разыменование в unsafe. Эталонное решение.

pub fn read_via_raw(x: i64) -> i64 {
    let p: *const i64 = &x; // создание указателя безопасно
    unsafe { *p } // разыменование - через дверцу unsafe; p валиден, x жив
}
