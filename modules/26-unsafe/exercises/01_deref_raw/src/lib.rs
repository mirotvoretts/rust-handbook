//! 01 (0x) - сырой указатель и разыменование в unsafe.
//!
//! Реализуй `read_via_raw(x)`, который получает значение `x`, строит из него сырой
//! указатель `*const i64` и возвращает результат его разыменования. Само создание
//! указателя безопасно; разыменование выполняй в `unsafe`-блоке (раздел 2 README).
//!
//! read_via_raw(42) == 42.

/// Построй `*const i64` из `x` и верни разыменованное значение.
pub fn read_via_raw(x: i64) -> i64 {
    todo!()
}
