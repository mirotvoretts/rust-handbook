//! 02 (0x) - запись через `*mut`.
//!
//! Реализуй `set_via_raw(target, value)`, который получает `&mut i64`, строит из него
//! сырой указатель `*mut i64` и записывает `value` через разыменование указателя в
//! `unsafe`-блоке (раздел 2 README). Функция ничего не возвращает - результат виден
//! через изменённый `target`.

/// Запиши `value` в `*target` через `*mut i64`.
pub fn set_via_raw(target: &mut i64, value: i64) {
    todo!()
}
