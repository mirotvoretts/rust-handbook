//! 03 (1x) — Структура с двумя независимыми временами жизни.
//!
//! `Report` ссылается на заголовок и на данные — источники у них разные, поэтому и времена
//! жизни разные: `'a` для заголовка, `'b` для данных. Экземпляр валиден, пока живы ОБА
//! источника. Параметры уже объявлены — реализуйте методы.

pub struct Report<'a, 'b> {
    pub title: &'a str,
    pub data: &'b [i32],
}

impl<'a, 'b> Report<'a, 'b> {
    pub fn new(title: &'a str, data: &'b [i32]) -> Report<'a, 'b> {
        todo!()
    }

    /// Заголовок отчёта.
    pub fn title(&self) -> &str {
        todo!()
    }

    /// Сумма данных.
    pub fn sum(&self) -> i32 {
        todo!()
    }

    /// Строка вида "Заголовок (N)", где N — число элементов данных.
    pub fn header(&self) -> String {
        todo!("соберите строку из title и числа элементов через format!")
    }
}
