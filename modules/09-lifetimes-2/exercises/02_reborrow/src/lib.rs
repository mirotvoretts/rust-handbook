//! 02 (1x) — Reborrow: переиспользование `&mut`.
//!
//! `&mut i32` не `Copy`, но его можно передавать в функцию много раз: при каждом вызове
//! компилятор делает REBORROW (`&mut *n`) — временный `&mut` на время вызова, после которого
//! исходный снова доступен. Реализуйте функции, опираясь на это.

/// Увеличивает значение на 1.
pub fn bump(x: &mut i32) {
    *x += 1;
}

/// Дважды вызывает `bump(n)`. Работает благодаря reborrow: `n` не перемещается.
pub fn bump_twice(n: &mut i32) {
    todo!("bump(n); bump(n);")
}

/// Вызывает `bump(n)` ровно `times` раз (reborrow на каждой итерации цикла).
pub fn bump_n(n: &mut i32, times: u32) {
    todo!()
}
