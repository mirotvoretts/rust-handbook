//! 05 (2x) - fn-указатель `fn(&str) -> usize` (неявный `for<'a>`).
//!
//! Тип `fn(&str) -> usize` - это fn-указатель, который обязан работать для ссылки ЛЮБОГО
//! времени жизни (по сути `for<'a> fn(&'a str) -> usize`). Поэтому его можно применять к разным
//! строкам без всяких аннотаций. Реализуйте все три функции.

/// Длина строки в байтах.
pub fn byte_len(s: &str) -> usize {
    todo!()
}

/// Число слов (разделённых пробелами) в строке.
pub fn word_count(s: &str) -> usize {
    todo!("s.split_whitespace().count()")
}

/// Применяет переданную функцию `f` к строке `s`.
pub fn apply(s: &str, f: fn(&str) -> usize) -> usize {
    todo!("f(s)")
}
