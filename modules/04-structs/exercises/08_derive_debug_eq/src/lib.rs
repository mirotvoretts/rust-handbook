//! 08 (1x) — Вывод типажей через `#[derive(...)]`.
//!
//! Сейчас у `Point` нет ни `Debug`, ни `PartialEq`, поэтому его нельзя ни сравнить через `==`,
//! ни напечатать через `{:?}`. ДОБАВЬТЕ `#[derive(Debug, PartialEq)]` над структурой, затем
//! реализуйте функции. (Тесты сравнивают `Point` напрямую и форматируют его — без derive они
//! даже не скомпилируются.)

// TODO: добавьте здесь #[derive(Debug, PartialEq)]
pub struct Point {
    pub x: i32,
    pub y: i32,
}

/// Равны ли две точки (используйте оператор `==`, который даёт `PartialEq`).
pub fn same(a: &Point, b: &Point) -> bool {
    todo!("a == b")
}

/// Отладочное представление точки через `{:?}`, например "Point { x: 1, y: 2 }".
pub fn debug_string(p: &Point) -> String {
    todo!("format! с :?")
}
