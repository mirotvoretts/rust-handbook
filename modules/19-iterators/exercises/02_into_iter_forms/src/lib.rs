//! 02 (0x) - Три формы обхода: iter / iter_mut / into_iter.
//!
//! Разница только в Item: &T / &mut T / T. Реши каждую функцию тем for-циклом
//! (или потребителем), который берёт нужную форму.

/// iter(): читаем, не трогая вектор. Верни сумму (вектор остаётся у вызвавшего).
pub fn sum_borrowed(v: &[i64]) -> i64 {
    todo!("for x in v { ... } - x: &i64, читаем через *")
}

/// iter_mut(): каждый элемент удвоить НА МЕСТЕ.
pub fn double_in_place(v: &mut [i64]) {
    todo!("for x in v.iter_mut() { *x *= 2; }")
}

/// into_iter(): потребляем вектор строк и склеиваем их в одну строку
/// (владение элементами переходит нам, лишних клонов не делаем).
pub fn concat_owned(v: Vec<String>) -> String {
    todo!("for s in v { out.push_str(&s); }")
}
