//! 01 (0x) - Ручной вызов next: лень и состояние итератора.
//!
//! Здесь НЕ используем for, map, sum и прочее - только `.iter()` / `.next()`,
//! чтобы прочувствовать: итератор помнит позицию и отдаёт Option.

/// Верни первые два элемента среза как кортеж, дёрнув next дважды.
/// Считай, что элементов минимум два.
pub fn first_two(v: &[i32]) -> (i32, i32) {
    todo!("let mut it = v.iter(); (*it.next().unwrap(), *it.next().unwrap())")
}

/// Сколько раз next вернёт Some, прежде чем впервые вернуть None.
/// Считай вручную в цикле while let Some(_) = it.next() - без .count().
pub fn count_manually(v: &[i32]) -> usize {
    todo!("while let Some(_) = it.next() { n += 1; }")
}

/// Итератор одноразов: пройди его до конца, посчитав элементы, и верни,
/// что вернёт next ПОСЛЕ исчерпания (должен быть None). Верни (кол-во, next_после).
pub fn drain_then_peek(v: &[i32]) -> (usize, Option<i32>) {
    todo!("исчерпай it, затем верни (n, it.next().copied())")
}
