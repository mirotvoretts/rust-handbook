//! 01 (0x) - async fn и .await: цепочка асинхронных стадий.
//!
//! Реализуй `run_chain(x)`, который прогоняет число через две асинхронные стадии:
//! сначала стадия A прибавляет 1, затем стадия B умножает результат на 2. Каждую
//! стадию оформи как `async fn` и соедини их через `.await` - выход A подай на вход B.
//! Само синхронное значение получи, запустив итоговый Future через block_on -
//! маленький executor, крутящий Future до готовности.
//!
//! run_chain(3) == (3 + 1) * 2 == 8.
//!
//! Конструкции за пределами теории раздела 4:
//! - запуск Future из синхронного кода: futures::executor::block_on
//!   (см. https://docs.rs/futures/latest/futures/executor/fn.block_on.html).

/// Прогони `x` через асинхронную стадию A (`+1`), затем стадию B (`*2`) и верни
/// итоговое число.
pub fn run_chain(x: i64) -> i64 {
    todo!()
}
