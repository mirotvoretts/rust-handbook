//! 02 (0x) - .await в цикле: последовательная обработка вектора.
//!
//! Реализуй `process_all(values)`, который каждое значение прогоняет через
//! `async fn`-преобразование (удвоение) и собирает результаты в новый вектор с
//! сохранением порядка. Преобразования выполняй последовательно: `.await` каждого
//! внутри цикла, до перехода к следующему. Итоговый Future запусти через block_on.
//!
//! process_all(vec![-2, 0, 5]) == vec![-4, 0, 10].
//!
//! Конструкции за пределами теории раздела 4:
//! - запуск Future из синхронного кода: futures::executor::block_on
//!   (см. https://docs.rs/futures/latest/futures/executor/fn.block_on.html).

/// Удвой каждое значение через асинхронное преобразование и верни результаты в
/// исходном порядке.
pub fn process_all(values: Vec<i64>) -> Vec<i64> {
    todo!()
}
