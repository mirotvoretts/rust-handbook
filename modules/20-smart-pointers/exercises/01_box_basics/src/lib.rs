//! 01 (0x) - Box: значение на куче, разыменование.
//!
//! Box<T> кладёт T в кучу; *b добирается до значения. Тут только Box::new и *.
// Box намеренно берётся по значению ради демонстрации - глушим совет clippy.
#![allow(clippy::boxed_local)]

/// Положи x в Box и верни его.
pub fn boxed(x: i32) -> Box<i32> {
    todo!("Box::new(x)")
}

/// Прочитай значение из Box обратно (разыменование).
pub fn unbox(b: Box<i32>) -> i32 {
    todo!("*b")
}

/// Сумма двух значений, каждое лежит в своём Box. Разыменуй оба.
pub fn sum_boxed(a: Box<i32>, b: Box<i32>) -> i32 {
    todo!("*a + *b")
}
